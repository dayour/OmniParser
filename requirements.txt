# OmniParser dependencies, Florence 3.1 baseline.
# See docs/specs/florence-3.1.md section 8 in the parent repository.
#
# Removed from the upstream list, each for a measured reason:
#   easyocr, paddlepaddle, paddleocr - replaced by the first-party omniparser-ocr
#       package (winrt backend). Installed by the parent repo from ../omniparser-ocr;
#       do NOT add it here by name, "omniocr" on PyPI is an unrelated third party.
#   ultralytics==8.3.70 - AGPL-3.0 and unused. util/yolov9.py imports no ultralytics
#       and omniparserserver.py defaults model_path=None, routing to YOLOv9Detector.
#   numpy==1.26.4 - an ABI-breaking downgrade for torch 2.13 and opencv 5.
#   opencv-python - duplicate of opencv-python-headless, the correct choice for a
#       server with no GUI.
#   dill - declared but imported nowhere in this repository.

torch>=2.13.0
torchvision>=0.28.0
transformers>=5.1.0
# transformers requires tokenizers >=0.22.0,<=0.23.0, but no 0.23.0 final exists on
# PyPI. 0.23.1 makes "import transformers" raise in dependency_versions_check.py.
tokenizers<=0.22.2
safetensors>=0.7.0
huggingface_hub>=1.26.1
accelerate>=1.14.0
timm>=1.0.28
einops>=0.8.0

numpy>=2.0
opencv-python-headless>=5.0.0
supervision>=0.30.0

openai>=2.53.0
anthropic[bedrock,vertex]>=0.37.1
azure-identity>=1.25.3
google-auth<3,>=2
boto3>=1.43.0
dashscope
groq

gradio>=6.22.0
streamlit>=1.38.0
jsonschema>=4.22.0
pyautogui==0.9.54
screeninfo
uiautomation

ruff==0.6.7
pre-commit==3.8.0
pytest==8.3.3
pytest-asyncio==0.23.6
